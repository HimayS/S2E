import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Prospera SMS Forwarder',
  webDir: 'dist'
};

export default config;
